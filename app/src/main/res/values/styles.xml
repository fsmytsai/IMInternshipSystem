<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorMainYellow</item>
        <item name="colorPrimaryDark">@color/colorMainYellow</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <!--<item name="tabMaxWidth">@dimen/design_tab_max_width</item>-->
        <item name="tabMaxWidth">700dp</item>
        <item name="tabSelectedTextColor">@color/colorMainYellow</item>
        <item name="tabTextColor">@android:color/black</item>
        <item name="tabIndicatorColor">@color/colorMainYellow</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabPaddingStart">12dp</item>
        <item name="tabPaddingEnd">12dp</item>
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="background">@android:color/black</item>
        <item name="tabTextAppearance">@style/MyCustomTextAppearance</item>
    </style>
    #tabTextAppearance引用到下方style

    <style name="MyCustomTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
    </style>
    #Tab的title是否全大寫，如果Tab上是放icon就設false

    <!-- spinner -->
    <style name="spinner_style">
        <item name="android:background">@drawable/spinner_selector</item>
        <item name="android:paddingLeft">5dip</item>
    </style>


</resources>
